# Rules

Repository-wide engineering and orchestration rules belong here.

Read `docs/spec-modes/` first when the project needs to choose between `LiteSpec` and `EnterpriseSpec` behavior.

Active quality rules:

- `architecture-boundaries.md` - dependency, composition, directory, and decomposition constraints.
- `quality-gates.md` - required checks, status vocabulary, blocking policy, and evidence contract.

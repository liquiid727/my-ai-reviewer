# QA Report — ROUND 2

**Spec:** AIP-010
**Issue:** #073 — Builder/privacy failing-test contract recovery (review fix round)
**Run ID:** 20260804T074413Z-qa-independent-073-r2
**Status:** GREEN
**Scope:** Independent re-execution of claimed ROUND 2 gates + P1/P2 contract readback. No implementation changes. No commit.

Claimed evidence: `tests/results/20260804T074102Z-aip010-issue-073-builder-privacy-test-baseline.json`
Prior ROUND 1 QA: `reviews/AIP-010/qa-issue-073-20260804.md` (GREEN @ 40 targeted / 240 full)

## Checks

| Check | Command | Result | Evidence |
|---|---|---|---|
| targeted-unit | `pytest …test_photo_rendering …test_resume_builder_services …privacy_builder_* …photo_api …photo_processor -q` | PASS | **48 passed** (was 40 in R1; +8 P1/P2 locks) |
| test-backend-unit | `pytest backend/tests/unit -q` | PASS | **218 passed** |
| test-backend-integration | `pytest backend/tests/integration -q` | PASS | **30 passed** (combined **248**) |
| lint-backend (scoped) | `ruff check backend/domain/resume_builder + imaging + rendering + photo/builder/privacy tests` | PASS | All checks passed |
| type-backend | `mypy backend --config-file backend/pyproject.toml` | PASS | 206 source files, no issues |
| diff-integrity | `git diff --check` | PASS | exit 0 |
| make type-check / arch-check / ci / test-unit | N/A targets | **NOT_RUN** | Makefile exposes `lint` / `test` only |

Independent counts match claimed baseline (48 / 248 / ruff / mypy / diff-check).

## P1 fix confirmation (review fix round)

| Contract | Code path | Test lock | Result |
|---|---|---|---|
| create keeps real placeholders (single sanitize) | `create_draft_from_profile` L443–463: `profile_to_draft` → **one** `_sanitize_draft_for_persistence` → persists that `privacy_manifest` | `test_profile_to_draft_maps_sections`, `test_double_sanitize_would_empty_manifest_but_create_path_does_not` | **PASS** |
| update doesn't wipe non-empty manifest | `update_draft` L594–612: sanitize then `_merge_privacy_manifests(existing, newly_redacted, content=…)` | `test_update_draft_does_not_wipe_existing_manifest`, `test_update_draft_merges_new_redactions_into_manifest` | **PASS** |
| export `allowed_tokens` = manifest ∪ content | `_allowed_tokens_for_export` L337–352; used by `export_draft_pdf` L747–751 | `test_allowed_tokens_union_manifest_and_content` | **PASS** |

## Prior #073 contracts still locked

| Contract | Evidence | Result |
|---|---|---|
| Photo preserve on update | `update_draft` strips client `photo`, restores `existing_photo`; `test_client_photo_stripped_and_existing_preserved` strong assert | **PASS** |
| Client photo stripped when no existing | `test_client_photo_stripped_when_no_existing` | **PASS** |
| Export does NOT auto-inject MinIO photo | `export_draft_pdf` takes request-scoped `photo_data_uri` only; no `draft_photo_data_uri`/`download_file` inside export; API GET/POST preview+export same; `test_export_does_not_auto_inject_persisted_identity_photo` tracks downloads == [] | **PASS** |
| Transient photo_data_uri forwarded, no storage | `test_transient_photo_data_uri_is_forwarded_without_storage` | **PASS** |
| Masked persistence / photo key protected | `_sanitize_draft_for_persistence` protects `photo`; `PrivacyGuard().assert_masked` on sanitize | **PASS** |
| Original tests not deleted/weakened | R1 strong asserts retained; R2 adds locks without removing prior cases | **PASS** |

## P2 / call-site checks

| Check | Result | Evidence |
|---|---|---|
| `set_draft_photo` rejects data URI | **PASS** | `_validate_photo_object_name` rejects `data:`, `base64,`, empty, whitespace, >512; `test_set_draft_photo_rejects_data_uri` + `test_validate_photo_object_name_*` |
| `set_draft_photo` accepts MinIO object name | **PASS** | `test_set_draft_photo_accepts_object_name` |
| No production `profile_to_draft` call without sanitize | **PASS** | Sole production call site: `services.py:445` inside `create_draft_from_profile`, immediately wrapped by `_sanitize_draft_for_persistence`. Other persist paths (`create_draft_from_reference`, `update_draft`) sanitize their own inputs. Tests call `profile_to_draft` only to assert the unsanitized→single-sanitize contract. |

## Findings

| ID | Severity | Location | Requirement | Finding | Recommendation |
|---|---|---|---|---|---|
| — | — | — | — | No P0–P3 findings from independent ROUND 2 verification | — |

## Residual risks (non-blocking)

1. Make target contract (`type-check` / `arch-check` / `ci` / `test-unit`) still absent — owned by later AIP-010 issues (#074+); marked **NOT_RUN**, not a #073 defect.
2. Reviewer P2 hybrid UX (full GET preview photo API/FE rewire) intentionally out of scope per issue notes.
3. Synthetic Chinese names in unit fixtures are intentional redaction inputs, not real PII.
4. `draft_photo_data_uri` still exists as a helper and still downloads from MinIO — but **export/preview paths do not call it**. Residual footgun if a future caller wires it into export; current contract tests would catch a regression on `export_draft_pdf`.

## Gate Decision

- Blocking findings: **none**
- Environment blockers: **none**
- Claimed evidence consistency: **match**
- Decision: **GREEN**

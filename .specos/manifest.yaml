project:
  name: specos-fullstack
  type: fullstack
projectMode: goalspec
stacks:
  frontend: next
  backend: node-api
artifacts:
  draftsDir: spec-draft
  specsDir: specs
  testsDir: tests
  resultsDir: tests/results
rulePacks:
  - spec-driven-delivery
agentTemplates:
  - fullstack-agent
workflows:
  - default-fullstack
ci:
  checkCommand: npx specos check

# Claude Code 执行提示词

请按依赖顺序完成 `tasks/issues/issue-092-*.md` 至 `issue-106-*.md`，严格遵循仓库 `AGENTS.md`、对应 PRD/SPEC 和每个 issue 的验收标准。逐项实现、测试并同步状态；保留现有未提交改动。全部完成后停止，不要 commit、push、创建或合并 PR，由 Fairy 负责 review-it 和 ship-it。
